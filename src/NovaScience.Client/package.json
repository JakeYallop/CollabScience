{
    "name": "nova-science",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "setup-https": "node aspnetcore-https.cjs && node aspnetcore-setup.cjs",
        "dev": "pnpm run setup-https && vite",
        "build": "tsc && vite build",
        "preview": "pnpm setup && vite preview"
    },
    "devDependencies": {
        "typescript": "^5.0.2",
        "vite": "^4.4.5"
    },
    "dependencies": {
        "@types/node": "^20.8.3"
    }
}
